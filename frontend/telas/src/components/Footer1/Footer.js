import React from 'react';

const Footer1 = () => {
  const footerStyle = {
    backgroundColor: '#89DFBB',
    color: 'white',
    textAlign: 'center',
    padding: '35px',
    position: 'fixed',
    left: 0,
    bottom: 0,
    width: '100%',
  };

  return (
    <footer style={footerStyle}>
    </footer>
  );
};

export default Footer1;
